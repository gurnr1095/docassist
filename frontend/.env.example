# Frontend environment variables
# For local dev, copy to .env and adjust as needed

# API base URL — leave as /api for local dev (Vite proxies to localhost:8000)
VITE_API_BASE_URL=/api

# Swagger docs link shown in the navbar
VITE_SWAGGER_URL=http://localhost:8000/docs
